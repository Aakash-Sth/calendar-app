import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const focusedDate = Color.fromARGB(255, 192, 32, 220);
  static Color selectedDate = Colors.blue.withOpacity(0.6);
  static const Color appBarColor = Color.fromARGB(255, 53, 50, 53);
  static const Color weekendColor = Color.fromARGB(255, 222, 35, 22);
}
